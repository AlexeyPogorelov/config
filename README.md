# My config for Linux
Clone this repo to `~/.config` folder.

### install software
- neovim
- nvm
- vifm
- i3-wm
- lazygit
- compton
- chromium

### install fonts
- `FontAwesome` for icons
- link compton config

### install env
- install `VimPlug` and plugins for `nvim`

